import styles from './styles.module.css'

const RunButton = ()=>{
    const handleClick = ()=>{
        
    }
    return(
       <button className={styles.runButton} onClick={handleClick}>Run</button>
    )
}
export default RunButton